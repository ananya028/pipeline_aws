version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu
hooks:
  BeforeInstall:
    - location: docker-install
      timeout: 800
      runas: root
  AfterInstall:
    - location: compose-run
      timeout: 900
      runas: root


